# Bootstrap Guidelines

`trellis init` 生成的首次填 spec 任务。FEATURE-349 已按本仓库真实分层写完 spec，本任务不再跟 frontend/backend 空模板。

## Status

- [x] desktop/app 规范（主 Electron + React 应用）
- [x] plugin-contract/shared
- [x] plugin-sdk/runtime
- [x] plugin-cli/cli
- [x] guides（task writing / TDD / reuse / cross-layer）
- [x] 示例插件不单独建 spec

后续增量改约定走 `trellis-update-spec`，或开新 task。
